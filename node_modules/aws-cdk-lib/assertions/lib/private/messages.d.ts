import { Messages } from './message';
export declare function findMessage(messages: Messages, logicalId: string, props?: any): {
    [key: string]: {
        [key: string]: any;
    };
};
export declare function hasMessage(messages: Messages, logicalId: string, props: any): string | void;
